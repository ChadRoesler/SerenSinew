version: 2
updates:
  # GitHub Actions used in .github/workflows/*. Dependabot rewrites the pinned
  # SHA (keeping the `# vX.Y.Z` comment in sync) when the action releases.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    commit-message:
      prefix: "ci"

  # Python deps for the meninges package (pyproject lives in SerenMeninges/).
  - package-ecosystem: "pip"
    directory: "/SerenMeninges"
    schedule:
      interval: "weekly"
    commit-message:
      prefix: "deps"